[server]
root_url = http://{{ hostvars[groups['web_servers'][2]].ansible_hostname }}/grafana/
serve_from_sub_path = true

[security]
admin_user = {{ grafana_user }}
admin_password = {{ grafana_password }}
